use strict;
use warnings;
package HTML::Abstract;
use Moose;

 # ABSTRACT: An abstraction of the HTML specification

# sub render {
#   my ($self) = @_;


#   foreach ( ) {
#     if ( ) {
#       $self->render();
#     }
#   }
# }

1;
